// Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

function baseExpoente (base, expoente) {
    return resolve =  Math.pow(base, expoente)


    /* Obs Professor Jair A//;
    O método MAth.pow já é considerado antigo, podemos usaar o exemplo abaixo citado na aula 104...
    let resultado = base ** expoente */ 

    //ex corrigido pela CODER.ORG

    
}  

console.log(baseExpoente(2, 2)) 